# Three-minute demo script

This is the exact recording order for the submission. Keep the browser on the
public Marketplace and use the terminal only for the local policy preview.

| Time | Show | Say |
| --- | --- | --- |
| 0:00–0:20 | `TreasuryPlanner → YieldScout / ChainSentinel` | “One buyer Agent hires two real seller workloads through the same bounded settlement layer.” |
| 0:20–0:55 | Live Judge Console: set task above budget, then Create | “The policy engine decides first. This request is BLOCKED locally, so the wallet never receives a write request.” |
| 0:55–1:25 | Restore defaults, connect wallet, create Testnet task | “With policy green, the judge can create a real Monad escrow task. Seller and verifier credentials never enter the browser.” |
| 1:25–1:55 | YieldScout 30 + ChainSentinel 29 | “Completed tasks commit real external data and pay only after task-specific independent verification.” |
| 1:55–2:20 | VERIFIED / BLOCKED / FROZEN cards | “The outcome is explicit: release after proof, stop before execution, or isolate bad output.” |
| 2:20–2:40 | 25-pipeline benchmark | “Monad handled 25 complete three-transaction authority pipelines: 25 verified, 75 unique transactions, 7.5 seconds P95.” |
| 2:40–2:55 | MetaMask Agent Wallet integration | “MetaMask isolates the self-custodial wallet; AgentGuard adds application policy, escrow and independent settlement proof.” |
| 2:55–3:00 | Boundary note | “This is a Testnet MVP: two-party recovery is live; neutral arbitration and audited production deployment remain future work.” |

Why Monad, in one sentence:

> Monad makes fine-grained Agent authority practical: escrow, result commitment
> and independent verification remain separate on-chain transitions, with 25
> complete Testnet pipelines measured under eight seconds at P95.

Do not call the local workflow LIVE_TESTNET. Do not call the live benchmark
protocol TPS, and do not claim that the MetaMask adapter broadcast without an
authenticated Agent Wallet session. Task 27's two-party refund is live; neutral
arbitration remains a production extension.
